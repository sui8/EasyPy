EasyPy 使い方
・準備
①インポートする。
一行目に
from EasyPy import EasyPy as py
と入力。

・機能
（基本的には、頭にpy.を付けてください。例えば、MessageBoxならpy.MessageBox）
〇MessageBox("種類", "タイトル", "文章")
種類は、info、warning、error、askquestion、askcancel、askyesno、asktrycancelがある。
返り値あり。

〇Dialog("タイトル", "文章")
返り値あり。
